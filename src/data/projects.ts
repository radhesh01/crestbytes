import type { Project } from '../types/project';

/**
 * Centralized project data. Populate with real projects only.
 * Never fabricate URLs, logos, or results.
 */
export const projects: Project[] = [];